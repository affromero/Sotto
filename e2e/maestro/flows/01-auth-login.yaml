appId: fm.sotto.app
name: Auth - Login and Verify Library
tags: [auth, critical]
---
- extendedWaitUntil:
    visible:
      id: 'login-email-input'
    timeout: 10000
- runFlow: ../helpers/login.yaml
- assertVisible:
    id: 'library-filter-all'
- assertVisible:
    id: 'library-podcast-list'
- takeScreenshot: after-login
- assertWithAI: 'A private podcast library with at least one podcast card is visible'
