appId: fm.sotto.app
name: Player - Playback Controls
tags: [player, smoke]
---
- runFlow: ../helpers/login.yaml

# Scroll to and tap seeded podcast
- scrollUntilVisible:
    element:
      id: 'podcast-card-${E2E_PODCAST_ID}'
    direction: DOWN
    timeout: 10000
- tapOn:
    id: 'podcast-card-${E2E_PODCAST_ID}'
- extendedWaitUntil:
    visible:
      id: 'player-play-pause'
    timeout: 5000
- assertWithAI: 'An audio player with playback controls is visible'

# Play / Pause
- tapOn:
    id: 'player-play-pause'
- tapOn:
    id: 'player-play-pause'

# Skip controls
- tapOn:
    id: 'player-skip-forward'
- tapOn:
    id: 'player-skip-back'

# Speed cycling
- tapOn:
    id: 'player-speed-button'
- tapOn:
    id: 'player-speed-button'

# Save toggle
- tapOn:
    id: 'player-save-button'
- tapOn:
    id: 'player-save-button'

# Share
- tapOn:
    id: 'player-share-button'

# Terminal assertion: player still functional
- assertVisible:
    id: 'player-play-pause'
- takeScreenshot: player-controls
