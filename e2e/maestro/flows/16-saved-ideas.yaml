appId: fm.sotto.app
name: Saved Ideas - Browse and Create from Idea
tags: [ideas, library]
---
- runFlow: ../helpers/login.yaml

# Navigate to ideas/library (accessible from profile)
- tapOn:
    text: "Profile"
- tapOn:
    id: "profile-saved-ideas-link"
- extendedWaitUntil:
    visible:
      id: "ideas-list"
    timeout: 5000

# Verify seeded ideas are visible
- assertVisible:
    text: "AI could write music"

# Tap on an idea to navigate to create
- tapOn:
    text: "AI could write music"

# Terminal assertion: create screen loaded
- extendedWaitUntil:
    visible:
      id: "create-chat-input"
    timeout: 5000
- assertVisible:
    id: "create-chat-input"
- takeScreenshot: idea-to-create
