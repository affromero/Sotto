appId: fm.sotto.app
name: Error - Generation Failure UI
tags: [error]
---
- runFlow: ../helpers/login.yaml

# Scroll to seeded FAILED-status episode
- scrollUntilVisible:
    element:
      id: "episode-card-${E2E_FAILED_EPISODE_ID}"
    direction: DOWN
    timeout: 10000
- tapOn:
    id: "episode-card-${E2E_FAILED_EPISODE_ID}"

# Verify error UI and retry button
- extendedWaitUntil:
    visible:
      id: "generation-error"
    timeout: 5000
- assertVisible:
    id: "generation-retry-button"
- takeScreenshot: generation-failure
