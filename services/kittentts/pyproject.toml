[project]
name = "kittentts-service"
version = "0.1.0"
description = "KittenTTS FastAPI sidecar for Sotto"
requires-python = "==3.12.*"
dependencies = [
    "kittentts==0.1.3",
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "soundfile==0.13.1",
    "numpy==2.2.2",
]

[tool.uv]
# Pin the index to avoid surprise upgrades
index-url = "https://pypi.org/simple"
