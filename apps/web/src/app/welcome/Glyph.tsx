/**
 * The welcome flow's Glyph is the shared aula icon set. Re-exported so existing
 * `./Glyph` imports keep working; the canonical implementation now lives in
 * `@/components/Glyph` and is shared with the admin console and learn surfaces.
 */
export { Glyph } from '@/components/Glyph';
