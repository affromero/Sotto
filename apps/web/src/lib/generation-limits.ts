/**
 * Plain generation ceilings. The self-hosted / BYOK product has no plans,
 * tiers, or quotas — these are simple safety limits applied uniformly.
 */
export const MAX_LESSON_DURATION_MINUTES = 40;
export const MAX_VOICE_CLONES = 10;
