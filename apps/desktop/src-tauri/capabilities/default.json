{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Sotto Host launcher window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
